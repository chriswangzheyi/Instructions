server:
  port: 5001

eureka:
  client:
    service-url:
      defaultZone: http://47.112.142.231:6001/eureka, http://47.112.142.231:6002/eureka
  instance:
    instance-id: gateway   #在信息列表显示主机名称
    prefer-ip-address: true  # 访问路径变为ip地址

spring:
  application:
    name: gateway
zuul:
  prefix: /
  ignored-services:
    "*"
  routes:
    provider: /p/**

